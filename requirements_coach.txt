# Coach Bot dependencies
python-telegram-bot>=20.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Optional: for payments
stripe>=7.0.0
requests>=2.31.0

# Database (if using SQLite, already built-in)
# For PostgreSQL, add: psycopg2-binary
